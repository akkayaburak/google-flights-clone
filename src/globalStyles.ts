import { css } from "@emotion/react";

export const globalStyles = css`
  body {
    margin: 0;
    padding: 0;
    background: linear-gradient(to bottom, #4a90e2, #145388);
    min-height: 100vh;
    font-family: "Arial", sans-serif;
    display: flex;
    justify-content: center;
    align-items: center;
  }
`;
